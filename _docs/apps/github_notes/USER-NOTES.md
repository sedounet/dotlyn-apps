ui/home screen:
* un fichier ajoute n'a [as de statut (local synced...)
*  changer statut modifié par local (un fichier local sera automatiquement sauvegarder avec l'auto SAVE) 
* sur les cartes de fichiers les trois petits points avec l'option duplicate on va y rajouter un petit menu qui va s'afficher avec une icône copier-coller et on va rajouter l'icône éditer et supprimer 

ui cartes de fichier :
* on pourrait mettre la date de dernière modification ou synchronisation du nom du fichier on considère que la création du fichier est l'heure de dernière modification 
* dans les paramètres dans la liste des fichiers on remet aussi ... avec le menu identique ce qu'on fait à home screen on fait un seul widget 

bug :
* dans les paramètres à l'édition d'un fichier après changement on ne peut pas cliquer sur save aucune modification dres paramètres de fichier ne sont enregistre ni n'apparaît 
* à l'ajout d'un fichier qui existe déjà sur GitHub on a bien un message d'erreur qui est affiché en double par contre au lorsque l'on sync sur un github le fichier de destination est remplacé par le fichier local et nous n'avons plus le menu de trois choix que nous devrions avoir 

workflow : 
* a la duplication d'un fichier on peu garder le même alias de fichier (son nom local)