name: dotlyn_core
description: Shared core logic for Dotlyn apps
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Add shared dependencies here

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
